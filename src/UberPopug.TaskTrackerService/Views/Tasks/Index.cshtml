@using UberPopug.TaskTrackerService.Users
@model List<UberPopug.TaskTrackerService.Tasks.Task>

@{
    ViewBag.Title = "Tasks";
    Layout = "_Layout";
}

<h2>Tasks tracker</h2>


<div class="container">
    <a asp-controller="Tasks" asp-action="NewTask" class="btn btn-success">Create new task</a>
    @if (User.IsInRole(Role.Manager.ToString()))
    {
        <a asp-controller="Tasks" asp-action="AssignTasks" class="btn btn-primary">Assign tasks</a>
    }
    <div class="row">
        <div class="col-12">
            <table class="table table-bordered">
                <thead>
                <tr>
                    <th scope="col">Desciption</th>
                    <th scope="col">Status</th>
                    <th scope="col">Assigned to</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var task in Model)
                {
                    <tr>
                        <td>@task.Description</td>
                        <td>@task.Status.ToString()</td>
                        <td>@task.UserEmail</td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>